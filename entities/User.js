export
default class User {
    constructor(token, user) {

        this.user.id = userId
        this.user.name = userName
        this.user.email = email

    }

    getToken() {
        return this.token;
    }

    setToken() {
        //???
    }

    hasToken() {
        if (user.token) {
            return true;
        }
    }

    getUserInfo(token) {

    }



    // getalldevices() {
    //     return this.devices;
    // }
}

//getToken
//setToken
//hasToken
//getUserInfo
//getalldevices
//getdevice??
//logout- move to util?